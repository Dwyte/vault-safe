{
  "name": "to-do-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "config": "^3.1.0",
    "cors": "^2.8.5",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "joi": "^14.3.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^5.6.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-start": "cd client && npm run build && cd .. && export NODE_ENV=production && nodemon",
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "push-heroku": "git push heroku master",
    "push-origin": "git push origin master"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
